const config = {
  // 启动端口
  port: 8080,
  // 数据库配置
  database: {
    DATABASE: 'eternal',
    USERNAME: 'root',
    PASSWORD: '1314520ch',
    PORT: '3306',
    HOST: 'localhost'
  }
}

module.exports = config